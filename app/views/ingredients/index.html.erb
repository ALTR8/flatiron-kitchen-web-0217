<h1>Ingredients</h1>
<p><% @ingredients.each do |ingredient| %>
	<li><%= link_to ingredient.name, ingredient_path(ingredient.id) %></li></p>
<%end%><br></br>
	<%= link_to 'create new ingredient', new_ingredient_path %>
